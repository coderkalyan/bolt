#version 450

layout(binding = 0, rgba8) uniform writeonly image2D dest;

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {
    ivec4 pixel = ivec4(255, 0, 0, 255);
    imageStore(dest, ivec2(gl_GlobalInvocationID.xy), pixel);
}
